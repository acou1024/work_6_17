# 自动化祈愿视频生成系统项目交接说明

更新时间：2026-06-17

## 1. 项目到底要做什么

这是一个部署在独立服务器上的内部 Web 工具，给员工通过浏览器使用。

员工只需要输入两项信息：

- 姓名，例如：张三
- 生辰，例如：1990年正月初一

系统自动生成一条祈愿视频，视频里会出现一张红纸，红纸上像真实写上去一样出现行书祈愿文字，同时配上定制语音。

最终员工得到的是一个可播放、可下载的 MP4 文件。

业务目标不是做一个普通字幕工具，而是做“视觉真实贴合”的祈愿视频自动生成系统：

- 文字必须像印在红纸/写在红纸上一样。
- 文字要跟着红纸的透视、位置、出画、入画变化。
- 红纸半出画时，文字也要被裁切/淡出，不能整块悬浮在画面上。
- 音频要使用音色克隆 TTS API，生成定制祈愿语音。
- 员工不需要懂剪辑、不需要打开 AE、不需要手工导出视频。

## 2. 员工最终应该怎么使用

员工访问公司内网或指定域名，例如：

```text
https://your-domain.com/prayer-video
```

页面上只保留极简操作：

1. 输入姓名。
2. 输入生辰。
3. 点击“一键生成祈愿视频”。
4. 等待 Loading，后端处理视频和音频。
5. 页面展示视频播放器。
6. 员工检查效果。
7. 点击“下载 MP4”。

员工不应该接触这些东西：

- AE 文本轨迹文件
- `trajectory.json`
- 模板视频路径
- 字体路径
- FFmpeg 命令
- TTS API 参数
- 服务器目录

## 3. 当前代码所在位置

当前工作目录：

```text
/Users/tt/Documents/社交聊天话术
```

核心文件：

```text
app.py
parse_two_ae_files.py
trajectory.json
requirements.txt
public/fonts/Xingkai.ttc
outputs/prayer-sim-preview-zhangsan-trajectory-movingavg-h264.mp4
```

当前最重要的源码：

- `app.py`：Streamlit Web 页面 + 视频文字渲染 + TTS 请求 + 音视频合并。
- `parse_two_ae_files.py`：把两个 AE Corner Pin 导出文本合并成 `trajectory.json`。
- `trajectory.json`：当前模板视频每一帧红纸四角坐标。后端渲染文字时靠它贴合红纸。
- `requirements.txt`：Python 依赖。

注意：当前目录里还有很多历史测试视频、图片、旧项目文件。正式交给程序员时，建议单独整理一个干净项目目录，不要把整个当前工作目录原样当生产仓库。

## 4. 当前已完成到什么程度

已经完成：

- Streamlit 页面雏形。
- 输入姓名、生辰。
- 用 Pillow 生成透明文字层。
- 使用行书字体 `public/fonts/Xingkai.ttc`。
- 按四行祈愿文案排版。
- 读取 AE 导出的 `trajectory.json`。
- 按帧把文字透视贴到红纸四角。
- 红纸出画时不绘制文字。
- 红纸半出画时根据可见面积淡出，避免文字漂浮。
- 7-10 秒附近如果轨迹缺失，可以根据最后有效坐标预测红纸继续滑出，不直接消失。
- 尾部做了防拉扯约束，降低第二段数据变形。
- 用接近正片叠底的方式融合黑字，让文字更像压在红纸纹理里。
- TTS API 调用函数已写好结构。
- FFmpeg 音视频合并函数已写好。
- 临时文件流程已写好。

当前最接近可用的视觉预览版本：

```text
outputs/prayer-sim-preview-zhangsan-trajectory-movingavg-h264.mp4
```

这个版本对应当前 `app.py` 逻辑，简称 `movingavg` 版本。

明确取消不用的版本：

```text
outputs/prayer-sim-preview-zhangsan-trajectory-globalstab-h264.mp4
```

`globalstab` 是后面试过的“全局 21 帧滤波 + 矩阵锁”版本，用户反馈不要用，当前代码已经回退，不再使用。

## 5. 当前模板视频与轨迹数据

当前默认模板视频：

```text
/Users/tt/Downloads/11954.MP4
```

视频信息：

```text
宽度：720
高度：1280
帧率：30 fps
总帧数：752
总时长：约 25.07 秒
```

当前轨迹文件：

```text
trajectory.json
```

轨迹统计：

```text
总帧数：752
有效坐标帧：370
None 帧：382
有效段：0-269 帧，600-699 帧
None 段：270-599 帧，700-751 帧
```

含义：

- 0-269 帧：前半段红纸在画面内或部分在画面内，可以贴字。
- 270-599 帧：红纸出画，不贴字。
- 600-699 帧：红纸再次出现，可以贴字。
- 700-751 帧：红纸再次出画或不可见，不贴字。

## 6. 文字内容与排版规则

透明文字图层大小：

```python
TEXT_CANVAS_SIZE = (900, 620)
```

四行文字：

```text
第一行：上香祈愿
第二行：{姓名} {生辰}
第三行：祈福：身体健康，工作顺利
第四行：万事顺遂，所愿皆成
```

字号逻辑：

- 标题最大 86，最小 68。
- 姓名生辰最大 52，最小 34。
- 后两行最大 46，最小 32。
- 如果姓名或生辰较长，会自动缩小字号，避免越界。

字体：

```text
public/fonts/Xingkai.ttc
```

文字颜色：

- 主体黑字：RGBA `(0, 0, 0, 235)`
- 轻微阴影：RGBA `(0, 0, 0, 42)`

## 7. 当前视觉贴图算法

当前使用的是 AE 轨迹驱动，而不是自动识别红纸。

流程：

1. AE 师傅对模板视频中的红纸做 Corner Pin 追踪。
2. 导出两个 AE Corner Pin 文本文件。
3. `parse_two_ae_files.py` 把两个文本文件合并成 `trajectory.json`。
4. `app.py` 逐帧读取 `trajectory.json`。
5. 当前帧如果是 `None`，说明红纸不在画面里，直接写原视频帧。
6. 当前帧如果有四角坐标，就用 `cv2.getPerspectiveTransform` 计算透视矩阵。
7. 用 `cv2.warpPerspective` 把文字层贴到红纸四角上。
8. 用 `multiply_like_blend` 做类似正片叠底融合。

当前实际渲染链路：

```text
trajectory.json
-> 5 帧居中滑动平均
-> 连续有效段中值去尖刺
-> 中心点/形状分离的低通平滑
-> 7-10 秒缺帧时外推
-> 尾段防拉扯
-> 可见面积裁切/淡出
-> 透视变换贴图
-> 正片叠底式融合
```

## 8. 已经试过但放弃的方案

这些方案都试过，最终不采用：

1. 固定四角坐标。
   - 问题：手持视频红纸一直动，文字不跟着纸动，会悬浮。

2. Lucas-Kanade 光流只追踪 4 个角。
   - 前 3-4 秒效果好。
   - 后面纯色红纸缺乏特征，角点漂移，文字拉扯。

3. 多特征点 + RANSAC 单应性矩阵。
   - 理论上稳，但当前素材上效果更差。

4. 时间轴关键帧插值。
   - 因分辨率和时间戳偏差，失败。

5. HSV 红色轮廓检测。
   - 受红蜡烛、背景红色元素、画面遮挡影响，不够稳定。

6. 全局 21 帧滤波 + 矩阵锁。
   - 文件名 `globalstab`。
   - 用户反馈这个版本取消，不使用。

当前保留的是 `movingavg` 版本。

## 9. 当前 `app.py` 模块说明

### 9.1 配置区

重要环境变量：

```text
VIDEO_TEMPLATE_PATH
TRAJECTORY_JSON_PATH
XINGSHU_FONT_PATH
TTS_ENDPOINT
TTS_API_KEY
TTS_VOICE_ID
TRAJECTORY_SOURCE_WIDTH
TRAJECTORY_SOURCE_HEIGHT
```

当前默认值仍是本机开发路径，不适合直接上服务器：

```text
VIDEO_TEMPLATE_PATH=/Users/tt/Downloads/11954.MP4
TRAJECTORY_JSON_PATH=项目目录/trajectory.json
XINGSHU_FONT_PATH=项目目录/public/fonts/Xingkai.ttc
```

部署到阿里云时必须改成服务器路径，例如：

```text
VIDEO_TEMPLATE_PATH=/opt/prayer-video/assets/11954.MP4
TRAJECTORY_JSON_PATH=/opt/prayer-video/assets/trajectory.json
XINGSHU_FONT_PATH=/opt/prayer-video/assets/Xingkai.ttc
```

### 9.2 `create_text_layer`

负责生成透明 RGBA 文字图层。

输入：

- `name`
- `birthdate`
- `font_path`

输出：

- Pillow `Image.Image`，RGBA 透明背景。

### 9.3 `load_trajectory`

读取 `trajectory.json`，转成：

```python
dict[int, Optional[np.ndarray]]
```

每帧值：

- 有坐标：`np.ndarray`，形状 `(4, 2)`。
- 红纸不可见：`None`。

四角顺序必须是：

```text
左上、右上、右下、左下
```

### 9.4 轨迹平滑

当前包含：

- `centered_moving_average_trajectory`
  - 5 帧居中滑动平均。
  - 窗口里遇到 `None` 会跳过。

- `smooth_loaded_trajectory`
  - 先中值滤波去掉尖刺。
  - 分离纸张中心点和四角形状。
  - 中心点轻平滑，形状重平滑。
  - 避免文字局部抖动和透视扭动。

- `smooth_trajectory_corners`
  - 渲染循环里再做一层轻量 EMA。
  - 大运动立即跟随，小运动低通。

### 9.5 出画与淡出

相关函数：

- `visible_area_ratio`
- `opacity_from_visible_ratio`

逻辑：

- 计算红纸四边形与视频画面的相交面积。
- 可见比例低于 `FADE_MIN_VISIBLE_RATIO=0.16` 时透明度为 0。
- 可见比例高于 `FADE_FULL_VISIBLE_RATIO=0.82` 时正常显示。
- 中间区域平滑渐变。

这样红纸半出画时文字也跟着淡出，不会漂在香炉或背景上。

### 9.6 7-10 秒缺帧外推

相关参数：

```text
HOLD_VISIBLE_START_SECONDS=7.0
HOLD_VISIBLE_END_SECONDS=10.0
HOLD_REFERENCE_SECONDS=6.0
MAX_EXTRAPOLATE_SECONDS=1.5
```

逻辑：

- 7-10 秒附近如果 JSON 中没有坐标，不是简单消失。
- 系统会用最后两组有效坐标估算红纸继续移动，让文字跟着滑出。
- 最多外推约 1.5 秒。

### 9.7 尾段防畸变

相关函数：

- `quad_shape_stats`
- `build_rigid_reference_quad`
- `constrain_tail_distortion`

逻辑：

- 20 秒后第二段红纸重新出现。
- 如果 AE 坐标导致长宽比、边长比例、对角线比例异常，会向第一帧更刚性的矩形比例收敛。
- 目的是避免尾部文字倒梯形拉扯。

### 9.8 融合方式

函数：

```python
multiply_like_blend(frame, warped, opacity=0.85)
```

逻辑：

- 使用透明通道提取文字 alpha。
- alpha 轻微高斯模糊，让边缘不那么硬。
- 黑色文字用类似正片叠底方式压进画面。

## 10. TTS 音频模块现状

代码已经有函数：

```python
generate_tts_audio()
```

当前只是通用 API 模板，真正上线时必须根据实际 TTS 厂商修改 payload。

当前生成的文案：

```text
上香祈愿。弟子 {姓名}，生辰 {生辰}。祈福：身体健康，工作顺利，万事顺遂，所愿皆成。
```

当前保留变量：

```text
TTS_ENDPOINT
TTS_API_KEY
TTS_VOICE_ID
```

注意：

- 当前没有真实 TTS 配置。
- 如果 `TTS_ENDPOINT` 还是 `example.com`，会直接报错。
- 如果 API 返回 JSON 而不是音频，也会报错。
- 当前默认保存为 mp3。

程序员下一步要做：

1. 确认实际音色克隆服务商。
2. 按服务商文档改 payload。
3. 确认返回音频格式。
4. 加失败重试。
5. 加超时和错误提示。
6. 最好把 TTS 请求日志写入后台日志。

## 11. 音视频合并模块

函数：

```python
merge_video_and_audio()
```

使用 `ffmpeg-python`。

规则：

- 最终视频长度以原视频画面长度为准。
- 原视频本身不使用原声。
- 音频短于视频时，使用 `apad` 自动补静音。
- 音频长于视频时，用 `-t` 截断到视频长度。
- 视频编码：`libx264`
- 像素格式：`yuv420p`
- 音频编码：`aac`
- `movflags=+faststart`，方便浏览器在线播放。

## 12. 当前前端页面

当前前端使用 Streamlit。

页面很简单：

- 标题：祈愿视频生成系统
- 输入框：姓名
- 输入框：生辰
- 按钮：一键生成祈愿视频
- 成功后显示视频播放器
- 提供 MP4 下载按钮

当前没有：

- 登录
- 员工权限
- 历史记录
- 任务队列
- 多人同时生成排队
- 管理后台
- 模板选择
- 生成进度百分比

## 13. 临时文件与清理

当前 `generate_prayer_video()` 会创建临时目录：

```text
prayer_video_xxxxx/
```

里面会生成：

```text
01_silent_with_text.mp4
02_tts_audio.mp3
prayer_timestamp.mp4
```

Streamlit 页面读取最终视频 bytes 后，会删除临时目录。

这意味着：

- 员工下载按钮还可以用，因为文件内容已经在内存里。
- 服务器不会长期保留生成记录。
- 如果后续需要“历史订单/重新下载/审计”，必须增加持久化存储。

## 14. `parse_two_ae_files.py` 说明

用途：

把两个 AE Corner Pin 文本导出文件合并成 `trajectory.json`。

默认输入文件：

```text
/Users/tt/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/tengteng8124_edab/temp/drag/Adobe After Effects 6.0 Keyframe Da.txt
/Users/tt/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/tengteng8124_edab/temp/drag/Adobe After Effects 6.0 Keyframe Da1.txt
```

默认模板视频：

```text
/Users/tt/Downloads/11954.MP4
```

默认输出：

```text
trajectory.json
```

运行方式：

```bash
python parse_two_ae_files.py
```

或显式指定：

```bash
python parse_two_ae_files.py \
  --first-ae "/path/to/front.txt" \
  --second-ae "/path/to/back.txt" \
  --video "/path/to/template.mp4" \
  --output "./trajectory.json"
```

AE Corner Pin 点位顺序：

```text
AE 导出：左上、右上、左下、右下
OpenCV 需要：左上、右上、右下、左下
```

脚本里已经做了重排。

脚本里的出画段规则：

```text
gap_start_seconds = 10.0
gap_end_seconds = 20.0
```

也就是说 10-20 秒强制填 `None`。

另外，如果四边形在画面内可见面积太小，也会填 `None`，避免纸只露一条边时仍强行贴字。

## 15. 当前 requirements.txt

当前依赖：

```text
streamlit>=1.36.0
opencv-python-headless>=4.10.0.84
Pillow>=10.4.0
numpy>=1.26.4
requests>=2.32.3
ffmpeg-python>=0.2.0
python-dotenv>=1.0.1
```

服务器还必须安装系统级 FFmpeg。

Ubuntu/阿里云示例：

```bash
sudo apt update
sudo apt install -y ffmpeg python3-venv python3-pip
```

## 16. 阿里云部署建议

建议目录：

```text
/opt/prayer-video/
  app.py
  parse_two_ae_files.py
  requirements.txt
  .env
  assets/
    11954.MP4
    trajectory.json
    Xingkai.ttc
  outputs/
  logs/
```

建议 `.env`：

```text
VIDEO_TEMPLATE_PATH=/opt/prayer-video/assets/11954.MP4
TRAJECTORY_JSON_PATH=/opt/prayer-video/assets/trajectory.json
XINGSHU_FONT_PATH=/opt/prayer-video/assets/Xingkai.ttc
TTS_ENDPOINT=https://真实TTS服务地址
TTS_API_KEY=真实API_KEY
TTS_VOICE_ID=真实VOICE_ID
TRAJECTORY_SOURCE_WIDTH=720
TRAJECTORY_SOURCE_HEIGHT=1280
```

安装：

```bash
cd /opt/prayer-video
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

测试运行：

```bash
streamlit run app.py --server.address 0.0.0.0 --server.port 8501
```

生产建议：

- 用 `systemd` 或 `supervisor` 守护 Streamlit。
- 用 Nginx 反向代理到 `127.0.0.1:8501`。
- 配置 HTTPS。
- 限制只允许公司内部访问，或增加登录。
- 如果员工多，建议改成 FastAPI + 队列，不要长期只靠 Streamlit 单进程。

## 17. 程序员接手后最应该做的事

第一阶段：整理可部署版本。

1. 从当前目录里复制最小文件集到干净项目：
   - `app.py`
   - `parse_two_ae_files.py`
   - `requirements.txt`
   - `trajectory.json`
   - `public/fonts/Xingkai.ttc`
   - 模板视频 `11954.MP4`

2. 新建专用 `.env.example`，不要使用当前旧项目残留的 `.env.example`。

3. 把默认路径全部改成服务器相对或环境变量路径。

4. 确认 `movingavg` 是当前采用版本，不要继续使用 `globalstab`。

第二阶段：接入真实 TTS。

1. 选择 TTS/音色克隆服务商。
2. 根据服务商 API 改 `generate_tts_audio()`。
3. 用真实姓名生辰测试。
4. 确认音频长度、音质、格式。

第三阶段：生产化。

1. 加登录或内网限制。
2. 加任务队列，避免多人同时生成时卡死。
3. 加生成日志。
4. 加错误提示。
5. 加输出文件留存策略。
6. 加后台清理旧文件。
7. 加监控和磁盘空间报警。

第四阶段：视觉精修。

1. 继续用当前 `movingavg` 版本作为基线。
2. 如果仍有轻微打滑，优先让 AE 师傅重新导出更稳的 Corner Pin 数据，而不是盲目改 OpenCV 算法。
3. 可以在 AE 中尽量让红纸四角坐标贴真实纸角，不要贴背景边缘。
4. 如果某些帧纸角被遮挡，建议手动修轨迹，而不是完全依赖自动滤波。

## 18. 当前已知问题和风险

1. TTS 还没接真实厂商。
2. 当前 Web 没有登录权限。
3. 当前没有任务队列，多员工同时生成可能阻塞。
4. 当前没有历史记录。
5. 当前没有稳定的生产目录结构。
6. 当前 `.env.example` 不是这个项目专用。
7. 当前素材和输出文件很多，目录比较乱。
8. 当前视频视觉基本接近，但如果要求电影级真实，仍需要 AE 轨迹继续人工精修。
9. 模板视频一旦更换，必须重新做 AE 追踪并重新生成 `trajectory.json`。
10. 字体文件必须确认商业授权。
11. TTS 音色克隆也要确认授权和合规。

## 19. 给程序员的一句话结论

这个项目不是普通的“给视频加字幕”，而是一个“员工输入姓名生辰后，自动生成带真实红纸透视贴字和定制语音的祈愿视频”的内部 Web 生产工具。

当前最接近可用的版本是：

```text
app.py + trajectory.json + movingavg 视觉算法
```

不要使用：

```text
globalstab 版本
```

接手时优先做：

```text
干净项目结构 -> 服务器路径配置 -> 接真实 TTS -> 内网部署 -> 员工测试 -> 再做视觉精修
```

